{
  "type": "postgres",
  "url": "postgres://admin:example@localhost:5432/famulous",
  "synchronize": true,
  "logging": true,
  "entities": ["server/infra/entities/**/*.ts"],
  "subscribers": ["server/infra/subscriber/**/*.ts"],
  "migrations": ["server/infra/migrations/**/*.ts"],
  "cli": {
    "migrationsDir": "server/infra/migrations"
  }
}
